#include <stdio.h>

/**
 * main - this function prints a sentence followed by a new line
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
